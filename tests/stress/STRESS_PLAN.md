# 真机压测与验收矩阵（发布前必过）

自动化部分：`hdc_smoke.sh`（安装/启动/进程存活/崩溃/关键日志）。
以下为人工压测矩阵，全部通过前不宣称"发布就绪"。

## A. 队列与取消

| # | 场景 | 预期 |
|---|---|---|
| A1 | 连续入队 20 个 1080p 视频转码 | 串行执行、进度单调、通知栏进度正确 |
| A2 | 运行中点"停止" | 状态经"正在停止"→"已取消"，无残缺输出文件，队列继续下一项 |
| A3 | 取消后立即重试 | 可重试成功，不复用旧输出 |
| A4 | 20 任务执行中杀进程 → 重启 | 运行中任务标失败，等待任务自动恢复执行 |
| A5 | 队列暂停后入队 5 个 | 不启动；继续后按序执行 |

## B. 边界与资源

| # | 场景 | 预期 |
|---|---|---|
| B1 | 4K HEVC 10 分钟大文件转码 | 无 OOM；后台切换不中断（长时任务生效） |
| B2 | 存储不足 200MB 时压目标体积 | 任务失败且消息可读，无 0 字节输出残留 |
| B3 | 损坏文件（截断 MP4 / 伪扩展名） | 分析或执行阶段明确失败，不崩溃 |
| B4 | 500 张图批量转 WebP | 全部完成，输出文件名无碰撞 |
| B5 | 连续 10 次 GIF 目标体积迭代 | 每轮真实产出、进度映射正确、参数还原 |

## C. 编码器探测（本轮新增能力）

| # | 场景 | 预期 |
|---|---|---|
| C1 | 随包二进制进入音频工作台 | "转 MP3"显示"缺少编码器"，hilog 有 `probe libmp3lame=false` |
| C2 | 替换含 libmp3lame 的重构建二进制 | MP3/WebM/动态 WebP 自动点亮，实际转码成功 |
| C3 | 探测期间立即入队其他任务 | 探测与任务不互锁，任务正常执行 |

## D. 后台与实况窗

| # | 场景 | 预期 |
|---|---|---|
| D1 | 转码中回桌面 3 分钟 | 进度持续（通知栏），回前台状态一致 |
| D2 | 实况窗未获准入设备 | 静默降级，无报错弹窗 |
| D3 | 低电量模式执行队列 | 明显降速但可完成，无系统强杀 |

## E. 上架项

| # | 场景 | 预期 |
|---|---|---|
| E1 | 首页"隐私声明/开源许可" | 能打开并完整滚动查看 |
| E2 | 卸载重装 | 任务历史清空，无残留授权错误 |
| E3 | 覆盖安装升级 | 老历史可读，不崩溃 |
